import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  actionCard: {
    width: '48%',
    backgroundColor: '#FFF',
    padding: 16,
    borderRadius: 12,
    alignItems: 'center',
    borderWidth: 0.5,
    borderColor: '#CCC'
  },
  actionLabel: {
    marginTop: 8,
    fontWeight: '600',
  },
});

export default styles;